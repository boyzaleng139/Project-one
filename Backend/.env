# PostgreSQL connection string
# Format: postgresql://USERNAME:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://postgres:postgres123@localhost:5432/oven_dashboard

# Server
PORT=3001
FRONTEND_URL=http://localhost:3000

# Mock generator interval (milliseconds) — 2000 = every 2 seconds
MOCK_INTERVAL_MS=2000
